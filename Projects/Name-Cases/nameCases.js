var person = "shariq";
console.log(person.toUpperCase());
console.log(person.toLowerCase());
console.log(person.charAt(0).toUpperCase() + person.slice(1));
// The third one is a little complicated because js doesn't have any built in methods to capitalize strings